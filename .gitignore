# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Infracost output files
reports/
.infracost/usage.gob
.infracost/pricing.gob
.infracost/terraform_modules/
infracost-*
cost-report-*
*.json.diff

# Tag compliance reports
reports/tag-compliance/
*.compliance.html
*.compliance.json
*.compliance.csv

# Pre-commit
.pre-commit.log

# TFLint
.tflint.d/
.tflint-cache/
.tflint-test.hcl
.tflint-backup.hcl
.tflint-simple.hcl
*.tflint-*

# IDEs and editors
.vscode/launch.json
.vscode/tasks.json
.vscode/*.code-workspace
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Node.js (for markdownlint and other tools)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python (for pre-commit tools)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environment variables
.env
.env.local
.env.*.local

# AWS credentials
.aws/
aws-credentials

# Secrets and certificates
*.pem
*.key
*.crt
*.csr
secrets.yaml
secrets.yml

# Backup files
*.bak
*.backup
*-backup.*
*-test.*
*-temp.*
*-tmp.*

# Documentation temporales
*-SUMMARY.md
*-CONSOLIDATION.md
TFLINT-*.md
CONSOLIDATION-*.md

# Cache directories
.cache/
.pytest_cache/
.tox/
.coverage
.nyc_output

# Terraform cache específico
.terraform-validate-cache
.terraform-modules-cache/
**/.terraform.lock.hcl.backup

# Local development
local/
dev/

# Auto-tagger logs
auto-tagger.log
tag-*.log
