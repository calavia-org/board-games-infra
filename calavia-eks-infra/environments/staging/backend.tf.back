terraform {
  backend "s3" {
    bucket         = "calavia-eks-infra-staging"
    key            = "terraform/state"
    region         = "us-west-2"
    use_lockfile   = true
    encrypt        = true
  }
}
